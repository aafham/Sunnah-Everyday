# Staging only

This directory is intentionally empty. It is not an app asset, public bundle or
release input. Any future non-public fixture must be non-claiming and contain
the exact marker `KANDUNGAN DEMO — TIDAK UNTUK PENERBITAN` in its own metadata.

Do not place copied source text, reviewer identity/contact data, permission
documents, unpublished translations, or approval claims here. The ignore rules
require an explicit, reviewed exception before a candidate can be committed.
