# Sunnah Everyday Admin

Flutter web administration shell. It deliberately shows no content until
Supabase authentication, roles, source records and review workflow are built.

```powershell
flutter pub get
flutter run -d chrome
```

Run `flutter analyze`, `flutter test`, and `flutter build web` from this
directory before an admin-shell release candidate.
