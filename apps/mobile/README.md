# Sunnah Everyday Mobile

Android-first Flutter application with a safe no-content state until approved
content bundles exist. Run locally:

```powershell
flutter pub get
flutter run
```

Run quality checks from this directory with `flutter analyze` and `flutter test`.
The Android application ID is `com.aafha.sunnaheveryday`.
