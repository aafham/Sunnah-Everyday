import 'package:flutter/material.dart';

/// Shared colour tokens. Muted gold is reserved for non-body-text accents.
abstract final class SunnahColors {
  static const warmIvory = Color(0xFFF7F4EC);
  static const deepForest = Color(0xFF123F35);
  static const sage = Color(0xFFA8BFAF);
  static const mutedGold = Color(0xFFC6A15B);
  static const charcoal = Color(0xFF17211D);
  static const darkBackground = Color(0xFF0D1714);
}
